##

just some tools I created for other purposes and belong nowhere else

use freely if needed

## Content:

* schema_to_interface: taking a string version of python sql schema and making it inro a .ts interface draft (runnable func: make_interface(str))